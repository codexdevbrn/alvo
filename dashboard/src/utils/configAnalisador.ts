/** Padrões de corte/segmentação, compartilhados entre a tela de Cortes (que edita
 *  e salva) e a de Relatórios (que só lê o config.json salvo para gerar análises). */
export const CORTES_CLIENTES_PADRAO: [number, number, number] = [30, 50, 60];
export const CORTE_PRODUTOS_PADRAO = 80;
export const MAX_POR_GRUPO_PADRAO = 20;
