# Python
__pycache__/
*.pyc

# Credenciais locais (conexão Postgres, etc.)
.env
backend/.env

# Exports brutos do ERP (muito grandes para o GitHub — limite de 100 MB por arquivo)
*.dw_2d

# Backup gerado pelo harmonizar_descricoes.py
Base.antes-harm.csv

# Configurações locais de ferramentas
.claude/
*.dpapi

# Pastas de dados locais (não versionar — muito grandes)
dados-trabalho/
base-clientes/

# Artefatos graphify / Cursor
graphify-out/
.cursor/
.tmp_graphify.txt

# Configurações locais de outros assistentes (mesmo motivo de .claude/)
.agents/
.codex/

# Projeto separado, com repositório git próprio — não versionar aqui dentro
PrismSystem/

# Logs do agendador noturno
logs_agendador/

# Log de runtime (uvicorn / executável) e lixo local de build
backend/logs/
build_run.log

# Artefatos de empacotamento (PyInstaller / release)
dist_pyinstaller/
build_pyinstaller/
dist_release/

# Artefatos temporários de validação e capturas locais
/.recovery-*/
/backend/.tmp-*/
/.pytest_cache/
/backend/.pytest_cache/
/clientes-print.png

# PDFs de amostra gerados por relatorio_cliente.py
output/
